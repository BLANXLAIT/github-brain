{
  "name": "github-brain",
  "version": "0.0.1",
  "private": true,
  "description": "GitHub App backed by a Claude Managed Agent",
  "workspaces": [
    "cdk",
    "agent"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
