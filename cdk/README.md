# cdk

API Gateway + Lambda that receives GitHub App webhooks, verifies the HMAC signature, filters to events the agent should handle, and starts a Managed Agent session.

Deploys to `blanxlait-ai` (`057122451218`, `us-east-1`).
