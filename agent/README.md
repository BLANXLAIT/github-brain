# agent

Managed Agent definition — system prompt and tool set.

v1 scope: on a Dependabot `pull_request.opened` or `check_suite.completed` event for `niemesrw/openbrain`, decide whether the PR is a patch bump on green CI (merge) or anything else (comment + flag for human).
